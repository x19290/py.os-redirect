from .osredirect import (
    redirect, Redirect, NULL_OUT, STDERR_BIT, STDERR, STDOUT, STDOUT_BIT,
)
(
    redirect, Redirect, NULL_OUT, STDERR_BIT, STDERR, STDOUT, STDOUT_BIT,
)  # to avoid "not used" warnings
